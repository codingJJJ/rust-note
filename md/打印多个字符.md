```rust
println!("{} of {:b} people know binary, the other half doesn't", 1, 2);
+ 打印10个空格后打印1
println!("{number:>width$}", number=1, width=10);
println!("{number:3>width$}", number=1, width=10);
```